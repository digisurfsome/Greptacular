# Jeremy Call Library — Inbox

Drop YouTube URLs below, one per line.
Format: `{url} {type}` where type = `cold` or `sales`
If type omitted, harvester will ask Claude to infer from title.

## URLs

<!-- Add URLs below this line -->
