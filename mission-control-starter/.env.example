# Mission Control Configuration
# No API keys needed — uses Claude subscription via ~/.claude/.credentials.json
# Just make sure you've run: claude login

# Plane Configuration (fill these in when setting up Plane integration)
# PLANE_API_URL=http://localhost:8080
# PLANE_API_TOKEN=your-plane-api-token
# PLANE_WEBHOOK_SECRET=your-webhook-secret
